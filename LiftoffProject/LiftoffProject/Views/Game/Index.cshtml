@model List<LiftoffProject.Models.Game>

    <h1>@ViewBag.Title</h1>

@if (Model.Count == 0)
{
    <p>No games to display</p>
}
else
{ 
    <table class="table">
        <tr>
            <th>Name</th>
            <th>Box Art</th>
        </tr>

        @foreach (Game game in Model)
        {
            <tr>
                <td><a asp-controller="Game" asp-action="Details" asp-route-gameId="@game.LocalId">@game.Name</a></td>
                @if (game.Cover != null)
                {
                <td><img src="@game.Cover.Url" alt="@game.Name" /></td>
                }
                else
                {
                    <td>No Box Art</td>
                }
                <td><a asp-controller="Game" asp-action="Delete" asp-route-gameId="@game.LocalId">Delete</a></td>
            </tr>
        }

    </table>
}
